{
  "name": "rex-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev",
    "build:binary": "bash scripts/build-binary.sh",
    "build:binary:all": "bash scripts/build-binary.sh --target=all"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  }
}
