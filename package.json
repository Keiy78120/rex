{
  "name": "rex-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  }
}
