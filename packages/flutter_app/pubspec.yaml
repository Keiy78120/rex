name: rex_app
description: REX - Claude Code companion. Health checks, memory, gateway, voice.
publish_to: 'none'
version: 7.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  macos_ui: ^2.0.9
  process_run: ^1.3.0
  provider: ^6.1.2
  intl: ^0.19.0
  url_launcher: ^6.3.1
  shared_preferences: ^2.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
